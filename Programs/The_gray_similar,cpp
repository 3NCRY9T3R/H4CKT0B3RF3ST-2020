#include <bits/stdc++.h>
#define ll long long
#define mod 1000000000
using namespace std;
#define fast ios_base::sync_with_stdio(false);cin.tie(NULL);
#define ulli unsigned long long int

int main()
{
    fast
    ulli n;
    cin>>n;
    ulli a[n];
     for(int i=0;i<n;i++)
    {
        cin>>a[i];
    }
    if(n>=130)
    {
        cout<<"Yes"<<endl;
        return 0;
    }
    else
    {
        for(int i=0;i<n;i++)
        {
            for(int j=i+1;j<n;j++)
            {
                for(int k=j+1;k<n;k++)
                {
                    for(int p=k+1;p<n;p++)
                    {
                        ulli q=a[i]^a[j]^a[k]^a[p];
                        if(q==0)
                        {
                            cout<<"Yes"<<endl;
                            return 0;
                        }
                    }
                }
            }
        }
            cout<<"No"<<endl;
    }
}
